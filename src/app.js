/* eslint-disable */
import "bootstrap";
import "./style.css";

window.onload = function GetDomain() {
  let pronouns = ["the", "our"];
  let adjs = ["great", "big"];
  let nouns = ["jogger", "racoon"];
  let webs = [".com", ".it", ".es"];

  for (i = 0; i < pronouns.length; i++) {
    let pronoun = pronouns[i];
  }
  for (j = 0; i < adjs.length; i++) {
    let adj = adjs[j];
  }
  for (x = 0; x < nouns.length; x++) {
    let noun = nouns[x];
  }
  for (y = 0; y < webs.length; y++) {
    let web = webs[y];
  }
  console.log(pronoun + adj + noun + web);
};
